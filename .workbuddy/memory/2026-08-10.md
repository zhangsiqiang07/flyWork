# 2026-08-10

## 完成：iOS 崩溃定位分析 PRD
- 盘点 flyWork（Electron+React19）已完成功能：工作区、Git 操作、AI 提交（Bug AI 后端）、自动化流程（白名单+流式）、本地 AI Agent 集成、命令中心、云效 DevOps 集成。
- 设计 PRD：DSYM + PLCrashReporter 原始 `.plcrash` 崩溃符号化定位。
- 关键技术方案：主进程 `src/main/services/crash/`（plcrash.js/dsym.js/symbolicate.js/report.js）；`plcrashutil convert --format=json` + `dwarfdump --uuid` 建索引 + `atos -arch -o -l <pc...>` 批量符号化；按 images[].uuid 自动匹配 dSYM；新增 crash-* IPC + flywork.* preload + `CrashAnalysis.jsx` 视图 + 侧边栏 `crash` 入口。
- 文档产物：`docs/CRASH_ANALYSIS_PRD.md`（v1.0 草案，待评审）。
- 注意：plcrashutil 需随包分发或引导下载；仅 macOS + Xcode CLT；本期不接云符号化，dSYM 不外发。

## PRD 修订 v1.1（评审意见）
- 用户三条意见：(1) plcrashutil 用 /Users/dimoo/bin/plcrashutil，确认可打包；(2) dSYM 不做索引，单次拖入路径定位即可；(3) 符号化参考 PetPal/scripts/symbolicate_plcrash.sh，但报告改成可视化过程。
- 核查：plcrashutil 为 universal(x86_64+arm64) 368KB，ad-hoc 签名（无 Team ID）→ 随包需 electron-builder extraResources + 构建期 Developer ID 重签（否则公证失败）。
- 真实工具链：plcrashutil convert --format=ios + Xcode symbolicatecrash（非 atos）；需完整 Xcode。支持 .xcarchive/.app.dSYM/.app.dSYM.zip。
- 删除持久符号库索引（FR-2 改为单次拖入 archive）；引擎复刻脚本流水线但产物改为「可视化流水线 + 结构化 CrashReport」；流式日志复用 automation-log-chunk 机制（新增 crash-log-chunk）。
- 已重写 docs/CRASH_ANALYSIS_PRD.md → v1.1。参考脚本路径：/Users/dimoo/Desktop/works/PetPal/scripts/symbolicate_plcrash.sh

## 实现：崩溃符号化核心闭环 M1-M3（PRD v1.1 落地）
- 评估 PRD 后澄清三点决策（用户全选推荐）：① 崩溃报告持久化用主进程独立文件 `~/.flywork/crash-reports.json`，与渲染进程全量覆盖写的 data.json 解耦；② 范围 M1-M3 核心闭环（M4 批量/AI 根因后置）；③ 立即 vendoring plcrashutil + 配 extraResources。
- 工具链：`/Users/dimoo/bin/plcrashutil` → `resources/plcrashutil`（universal，ad-hoc 签名，当前 notarize:false 可直接运行）；electron-builder.yml 新增 `extraResources` 落到 `Contents/Resources/plcrashutil`。
- 主进程 `src/main/services/crash/`：plcrash.js（convert+extractMeta+extractBinaryImages）、dsym.js（resolveArchive 支持 zip/xcarchive/dSYM + dsymUuids + matchUuids + cleanupTmpDir）、symbolicate.js（findSymbolicatecrash + symbolicate + parseThreads/parseSymbolicatedCrash）、report.js（listReports/getReport/saveReport/deleteReport/buildReport，独立 crash-reports.json）。
- IPC（setupIPC 注册）：crash-toolchain-check / crash-import / crash-symbolicate（4 步流水线，crash-log-chunk 流式 emit 步骤状态+stderr，stdout 捕获为结果）/ crash-list-reports / crash-get-report / crash-delete-report。关键操作写 audit.log。
- Preload：flywork.crashToolchainCheck/crashImport/crashSymbolicate/crashListReports/crashGetReport/crashDeleteReport/onCrashLogChunk。
- 前端 `views/CrashAnalysis.jsx`：工具链自检 badge + 拖拽/选择导入 .plcrash + 拖入符号文件 + 流水线步骤卡（4步 spin/success/failed）+ 流式控制台 + 报告视图（摘要/UUID匹配面板/线程栈帧可折叠/复制全文/导出JSON）+ 历史列表。深色主题，复用 CSS 变量。sandbox:false 确保拖拽 File.path 可用。
- 接入：Sidebar navItems 加 `crash`（CrashIcon 三角警告）；App.jsx lazy import + renderMainContent case 'crash'。
- 验证：`pnpm build` 三环境通过（main 94KB/preload 8.89KB/renderer CrashAnalysis chunk 36KB 独立 lazy）。crash 文件 lint 干净（no-unused-vars/no-empty/set-state-in-effect 全 0；prop-types 与既有代码库一致保留）。resolvePlcrashutil 开发态走 process.cwd()/__dirname 推算 resources/，打包态走 process.resourcesPath。
- 待办（M4，未做）：批量导入/聚类、AI 根因（复用 handleAskAI，当前为 mock）、公证阶段 plcrashutil Developer ID 重签。
